<UserControl x:Class="SortMyClips.SortMyClipsSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="600">
    <StackPanel Margin = "10">
        <TextBlock Text="Unsorted media path:"/>
        <Grid>
        <TextBox Text="{Binding Path =Settings.UnsortedPath}"/>
        <Button HorizontalAlignment="Right" Content="🗁" Command="{Binding BrowseUnsortedFolder}"/>
        </Grid>
        
        <TextBlock Text="Sorting target path:" Margin = "0 10 0 0"/>
        <Grid>
            <TextBox Text="{Binding Path =Settings.SortedPath}"/>
            <Button HorizontalAlignment="Right" Content="🗁" Command="{Binding BrowseSortedFolder}"/>
        </Grid>
        
        <Grid Margin = "0 10 0 0">
        <CheckBox IsChecked="{Binding Settings.ScreenshotsMovedCount}">
        <TextBlock HorizontalAlignment="Right" Text="Notification, if media was moved"></TextBlock>    
        </CheckBox>
        </Grid>
    </StackPanel>
</UserControl>